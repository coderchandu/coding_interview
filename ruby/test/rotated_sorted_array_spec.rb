require '../src/rotated_sorted_array.rb'

it "finds the minimum on a rotated sorted array with even number of elements" do
  puts RotatedSortedArray.find_min([4,5,6,7,8,1,2]).must_equal 1
end

it "finds the minimum on a rotated sorted array with odd number of elements" do
end


it "finds the minimum on a rotated sorted array with one element" do
end


it "finds the minimum on a rotated sorted array with two elements" do
end

it "finds the minimum even if array is not rotated" do 
end

it "finds the minimum if all elements in array is same" do 
end
